#include "main.h"
#include <stdio.h>
/**
 * print_rev - Prints a string in reverse
 * @s: The string to print
 *
 * Return: void
 */
void print_rev(char *s)
{
int d = 0;
while (s[d] != '\0')
{
c++;
}
for (d -= 1; d >= 0; d--)
{
_putchar(s[d]);
}
_putchar('\n');
}
